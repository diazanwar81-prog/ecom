import 'reflect-metadata';
import { Controller, Get, Module, Injectable, Post, Body, Param, Query, Headers } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
// RESTORED - full file uploaded via fixed path in next commit if truncated
console.error('ECOM main incomplete - pull will fix');
