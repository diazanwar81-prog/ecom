FROM node:22-alpine

WORKDIR /workspace
ENV COREPACK_ENABLE_DOWNLOAD_PROMPT=0

# FFmpeg for phase-C slideshow MP4 render
RUN apk add --no-cache ffmpeg

RUN corepack enable && corepack prepare pnpm@9.15.4 --activate

CMD ["sh"]
